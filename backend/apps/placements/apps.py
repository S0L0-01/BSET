from django.apps import AppConfig

class PlacementsConfig(AppConfig):
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'apps.placements'
    verbose_name = '4. Training & Placements (CRT)'
